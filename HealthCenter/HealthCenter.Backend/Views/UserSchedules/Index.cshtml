@model IEnumerable<HealthCenter.Domain.UserSchedule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Scheduler.ApplicationUser_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Scheduler.ApplicationUser_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserScheduleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserScheduleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserScheduleId })
        </td>
    </tr>
}

</table>
